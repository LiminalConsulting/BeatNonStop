# Copy to .env.sync (gitignored) and fill in after deploying the Worker.
# The /sync slash command sources this to post to the bot.

WORKER_URL="https://beatnonstop-bot.YOUR-SUBDOMAIN.workers.dev"
SYNC_API_KEY="paste-the-same-string-you-set-as-worker-secret"
